spring:
  mail:
    host: smtp.163.com #发送邮件服务器
    username: takeiteasy1987@163.com #发送邮件的邮箱地址
    password: FYEQTTKNRRFVAFFK #客户端授权码，不是邮箱密码,网易的是自己设置的
    properties.mail.smtp.port: 465 #465或者994
    from: takeiteasy1987@163.com # 发送邮件的地址，和上面username一致
    properties.mail.smtp.starttls.enable: true
    properties.mail.smtp.starttls.required: true
    properties.mail.smtp.ssl.enable: true
    default-encoding: utf-8
  datasource:
    url: jdbc:mysql://localhost:3306/test？characterEncoding=utf8&useSSL=true
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver

